#include <stdio.h>

int main(void)
{
    int scores[4];
    scores[0] = 83;
    scores[1] = 99;
    scores[2] = 30;
    scores[3] = 100;
    printf("average = %f\n",(scores[0] +scores[1] + scores[2] + scores[3]) / 4.0);

    
}